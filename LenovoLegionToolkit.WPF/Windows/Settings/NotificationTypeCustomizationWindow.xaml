<local:BaseWindow
    x:Class="LenovoLegionToolkit.WPF.Windows.Settings.NotificationTypeCustomizationWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:LenovoLegionToolkit.WPF.Windows"
    xmlns:resources="clr-namespace:LenovoLegionToolkit.WPF.Resources"
    xmlns:utils="clr-namespace:LenovoLegionToolkit.WPF.Utils"
    xmlns:wpfui="http://schemas.lepo.co/wpfui/2022/xaml"
    Width="680"
    MinWidth="600"
    MaxHeight="700"
    FlowDirection="{x:Static utils:LocalizationHelper.Direction}"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    SizeToContent="Height"
    WindowStartupLocation="CenterOwner">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <wpfui:TitleBar
            Grid.Row="0"
            CanMaximize="False"
            ShowMaximize="False"
            ShowMinimize="False"
            UseSnapLayout="False" />

        <wpfui:DynamicScrollViewer Grid.Row="1" Margin="12,0,12,0">
            <StackPanel x:Name="_cardsContainer" Margin="0,8,0,8" />
        </wpfui:DynamicScrollViewer>

        <Grid Grid.Row="2" Margin="12,8,12,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <wpfui:Button
                Grid.Column="0"
                MinWidth="80"
                Click="ResetButton_Click"
                Content="{x:Static resources:Resource.Reset}" />

            <wpfui:Button
                Grid.Column="2"
                MinWidth="80"
                Appearance="Primary"
                Click="CloseButton_Click"
                Content="{x:Static resources:Resource.Close}" />
        </Grid>
    </Grid>

</local:BaseWindow>
