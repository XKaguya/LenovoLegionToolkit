<UserControl
    x:Class="LenovoLegionToolkit.WPF.Controls.Dashboard.SensorsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="clr-namespace:LenovoLegionToolkit.WPF.Behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:resources="clr-namespace:LenovoLegionToolkit.WPF.Resources"
    xmlns:wpfui="http://schemas.lepo.co/wpfui/2022/xaml"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d"
    >

    <UserControl.Resources>
        <Style TargetType="Border" x:Key="SensorCardStyle">
            <Setter Property="Margin" Value="0,0,16,0"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="{DynamicResource CardStrokeColorDefaultBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="{DynamicResource ControlFillColorDefaultBrush}"/>
            <Setter Property="Width" Value="220"/>
            <Setter Property="Height" Value="170"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="CardTitleStyle">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="DataLabelStyle">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="DataValueStyle">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>

        <Style TargetType="ProgressBar" x:Key="CompactProgressBarStyle">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="6"/>
            <Setter Property="Margin" Value="4,0,0,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#0288D1"/>
        </Style>
    </UserControl.Resources>

    <wpfui:CardControl HorizontalAlignment="Center">
        <wpfui:CardControl.Content>
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Border x:Name="_cpuCard" Style="{StaticResource SensorCardStyle}">
                        <StackPanel Margin="12,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Style="{StaticResource CardTitleStyle}" Text="{x:Static resources:Resource.SensorsControl_CPU_Title}"/>
                                <TextBlock x:Name="_cpuCardName" Grid.Column="1" Style="{StaticResource DataLabelStyle}" Text="-" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="8,0,0,12"/>
                            </Grid>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Style="{StaticResource DataLabelStyle}" Text="{x:Static resources:Resource.SensorsControl_Utilization_Title}"/>
                                <TextBlock x:Name="_cpuUtilizationLabel" Grid.Column="2" Style="{StaticResource DataValueStyle}" Text="-" Margin="8,0,0,0"/>
                                <ProgressBar x:Name="_cpuUtilizationBar" Grid.Column="3" Style="{StaticResource CompactProgressBarStyle}" Value="0" Maximum="100"/>
                            </Grid>
                            <Grid Margin="0,6,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Style="{StaticResource DataLabelStyle}" Text="{x:Static resources:Resource.SensorsControl_CoreClock_Title}"/>
                                <TextBlock x:Name="_cpuCoreClockLabel" Grid.Column="2" Style="{StaticResource DataValueStyle}" Text="-" Margin="8,0,0,0"/>
                                <ProgressBar x:Name="_cpuCoreClockBar" Grid.Column="3" Style="{StaticResource CompactProgressBarStyle}" Value="0" Maximum="100"/>
                            </Grid>
                            <Grid Margin="0,6,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Style="{StaticResource DataLabelStyle}" Text="{x:Static resources:Resource.SensorsControl_Temperature_Title}"/>
                                <TextBlock x:Name="_cpuTemperatureLabel" Grid.Column="2" Style="{StaticResource DataValueStyle}" Text="-"/>
                                <ProgressBar x:Name="_cpuTemperatureBar" Grid.Column="3" Style="{StaticResource CompactProgressBarStyle}" Value="0" Maximum="100"/>
                            </Grid>
                            <Grid Margin="0,6,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Style="{StaticResource DataLabelStyle}" Text="{x:Static resources:Resource.SensorsControl_Fan_Title}"/>
                                <TextBlock x:Name="_cpuFanSpeedLabel" Grid.Column="2" Style="{StaticResource DataValueStyle}" Text="-"/>
                                <ProgressBar x:Name="_cpuFanSpeedBar" Grid.Column="3" Style="{StaticResource CompactProgressBarStyle}" Value="56" Maximum="5000"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                    <Border x:Name="_gpuCard" Style="{StaticResource SensorCardStyle}">
                        <StackPanel Margin="12,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Style="{StaticResource CardTitleStyle}" Text="{x:Static resources:Resource.SensorsControl_GPU_Title}"/>
                                <TextBlock x:Name="_gpuCardName" Grid.Column="1" Style="{StaticResource DataLabelStyle}" Text="-" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="8,0,0,12"/>
                            </Grid>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Style="{StaticResource DataLabelStyle}" Text="{x:Static resources:Resource.SensorsControl_Utilization_Title}"/>
                                <TextBlock x:Name="_gpuUtilizationLabel" Grid.Column="2" Style="{StaticResource DataValueStyle}" Text="-" Margin="8,0,0,0"/>
                                <ProgressBar x:Name="_gpuUtilizationBar" Grid.Column="3" Style="{StaticResource CompactProgressBarStyle}" Value="0" Maximum="100"/>
                            </Grid>
                            <Grid Margin="0,6,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Style="{StaticResource DataLabelStyle}" Text="{x:Static resources:Resource.SensorsControl_CoreClock_Title}"/>
                                <TextBlock x:Name="_gpuCoreClockLabel" Grid.Column="2" Style="{StaticResource DataValueStyle}" Text="-" Margin="8,0,0,0"/>
                                <ProgressBar x:Name="_gpuCoreClockBar" Grid.Column="3" Style="{StaticResource CompactProgressBarStyle}" Value="0" Maximum="100"/>
                            </Grid>
                            <Grid Margin="0,6,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Style="{StaticResource DataLabelStyle}" Text="{x:Static resources:Resource.SensorsControl_Fan_Title}"/>
                                <TextBlock x:Name="_gpuFanSpeedLabel" Grid.Column="2" Style="{StaticResource DataValueStyle}" Text="-"/>
                                <ProgressBar x:Name="_gpuFanSpeedBar" Grid.Column="3" Style="{StaticResource CompactProgressBarStyle}" Value="0" Maximum="5000"/>
                            </Grid>
                            <Grid Margin="0,6,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                           Style="{StaticResource DataLabelStyle}"
                                           Text="{x:Static resources:Resource.SensorsControl_GpuCore_Title}"
                                           VerticalAlignment="Center"/>
                                <TextBlock x:Name="_gpuCoreTemperatureLabel"
                                           Grid.Column="1"
                                           Style="{StaticResource DataValueStyle}"
                                           Text="-"
                                           Margin="4,0,8,0"
                                           VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="2"
                                           Style="{StaticResource DataLabelStyle}"
                                           Text="{x:Static resources:Resource.SensorsControl_GpuMemoryTemperature_Title}"
                                           VerticalAlignment="Center"
                                           Margin="8,0,0,0"/>
                                <TextBlock x:Name="_gpuMemoryTemperatureLabel"
                                           Grid.Column="3"
                                           Style="{StaticResource DataValueStyle}"
                                           Text="-"
                                           Margin="4,0,0,0"
                                           VerticalAlignment="Center"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                    <Border x:Name="_motherboardCard" Style="{StaticResource SensorCardStyle}">
                        <StackPanel Margin="12,10">
                            <TextBlock Style="{StaticResource CardTitleStyle}" Text="{x:Static resources:Resource.SensorsControl_Motherboard_Title}"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Style="{StaticResource DataLabelStyle}" Text="{x:Static resources:Resource.SensorsControl_PchTemperature_Title}"/>
                                <TextBlock x:Name="_pchTemperatureLabel" Grid.Column="2" Style="{StaticResource DataValueStyle}" Text="-"/>
                                <ProgressBar x:Name="_pchTemperatureBar" Grid.Column="3" Style="{StaticResource CompactProgressBarStyle}" Value="0" Maximum="100"/>
                            </Grid>
                            <Grid Margin="0,6,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Style="{StaticResource DataLabelStyle}" Text="{x:Static resources:Resource.SensorsControl_Fan_Title}"/>
                                <TextBlock x:Name="_pchFanSpeedLabel" Grid.Column="2" Style="{StaticResource DataValueStyle}" Text="-"/>
                                <ProgressBar x:Name="_pchFanSpeedBar" Grid.Column="3" Style="{StaticResource CompactProgressBarStyle}" Value="0" Maximum="5000"/>
                            </Grid>
                            <Grid Margin="0,6,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Style="{StaticResource DataLabelStyle}" Text="{x:Static resources:Resource.SensorsControl_VrTemperature_Title}"/>
                                <TextBlock x:Name="_vrTemperatureLabel" Grid.Column="2" Style="{StaticResource DataValueStyle}" Text="-"/>
                                <ProgressBar x:Name="_vrTemperatureBar" Grid.Column="3" Style="{StaticResource CompactProgressBarStyle}" Value="0" Maximum="100"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                    <Border x:Name="_memoryBatteryCard" Style="{StaticResource SensorCardStyle}">
                        <StackPanel Margin="12,10">
                            <StackPanel Margin="0,0,0,12">
                                <TextBlock Style="{StaticResource CardTitleStyle}" Text="{x:Static resources:Resource.SensorsControl_Memory_Title}" Margin="0,0,0,6"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Style="{StaticResource DataLabelStyle}" Text="{x:Static resources:Resource.SensorsControl_Utilization_Title}"/>
                                    <TextBlock x:Name="_memoryUtilizationLabel" Grid.Column="2" Style="{StaticResource DataValueStyle}" Text="-"/>
                                    <ProgressBar x:Name="_memoryUtilizationBar" Grid.Column="3" Style="{StaticResource CompactProgressBarStyle}" Value="0" Maximum="100"/>
                                </Grid>
                                <Grid Margin="0,6,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Style="{StaticResource DataLabelStyle}" Text="{x:Static resources:Resource.SensorsControl_Temperature_Title}"/>
                                    <TextBlock x:Name="_memoryTemperatureLabel" Grid.Column="2" Style="{StaticResource DataValueStyle}" Text="-"/>
                                    <ProgressBar x:Name="_memoryTemperatureBar" Grid.Column="3" Style="{StaticResource CompactProgressBarStyle}" Value="0" Maximum="100"/>
                                </Grid>
                            </StackPanel>
                            <Border Height="1" Background="{DynamicResource CardStrokeColorDefaultBrush}" Margin="0,0,0,12"/>
                            <StackPanel>
                                <TextBlock Style="{StaticResource CardTitleStyle}" Text="{x:Static resources:Resource.SensorsControl_Battery_Title}" Margin="0,0,0,6"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Style="{StaticResource DataLabelStyle}" Text="{x:Static resources:Resource.SensorsControl_State_Title}"/>
                                    <TextBlock x:Name="_batteryStateLabel" Grid.Column="2" Style="{StaticResource DataValueStyle}" Text="-"/>
                                </Grid>
                                <Grid Margin="0,6,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Style="{StaticResource DataLabelStyle}" Text="{x:Static resources:Resource.SensorsControl_BatteryLevel_Title}"/>
                                    <TextBlock x:Name="_batteryLevelLabel" Grid.Column="2" Style="{StaticResource DataValueStyle}" Text="-"/>
                                    <ProgressBar x:Name="_batteryLevelBar" Grid.Column="3" Style="{StaticResource CompactProgressBarStyle}" Value="0" Maximum="100"/>
                                </Grid>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </wpfui:CardControl.Content>
    </wpfui:CardControl>
</UserControl>